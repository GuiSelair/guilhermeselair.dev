overwrite: true

schema: ${NEXT_PUBLIC_GRAPHCMS_ENDPOINT}

documents: 'src/core/graphql/**/*.graphql'

generates:
  src/core/generated/sdk.ts:
    plugins:
      - 'typescript'
      - 'typescript-operations'
      - 'typescript-graphql-request'


